# OpenCBT — Next Phase Guide

## Status: Phase 0-11 roadmap complete

The original Feature Roadmap (`docs/FEATURE_ROADMAP.md`) ran Phase 0 through
Phase 11, and Phase 11 (Production Deployment prep) is done — see
`docs/CURRENT_STATUS.md` for exactly what that covers and what's still
outstanding (the actual Vercel/Supabase Cloud deploy itself needs the project
owner's accounts; steps are in `docs/DEPLOYMENT.md`).

There is no Phase 12 defined yet. Candidates already flagged under "Not
Started" in `docs/CURRENT_STATUS.md`:
- Guides module (currently a placeholder page)
- Admin role checks
- File upload UI for heritage media (currently URL-only)
- Per-community breakdown / charts on `/impact`
- Assistant conversation memory / streaming
- Fuzzy/ranked search

### For the Next AI/Session
Don't invent a new phase number or start building one of the above without
confirming scope with the project owner first — per the project's own rule,
"one phase at a time." Read `docs/CURRENT_STATUS.md` and
`docs/FEATURE_ROADMAP.md` first, agree on what "Phase 12" actually is, then
update this file with a proper goal/tasks/definition-of-done section before
writing code — the same way Phases 8-11 were scoped here in advance.
